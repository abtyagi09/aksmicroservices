apiVersion: apps/v1
kind: Deployment
metadata:
  name: loans-service
  namespace: loans-underwriting
spec:
  replicas: 2
  selector:
    matchLabels:
      app: loans-service
  template:
    metadata:
      labels:
        app: loans-service
    spec:
      containers:
      - name: loans
        image: fbdevygfwoiacr.azurecr.io/farmersbank/loansunderwriting:v1
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: loans-service
  namespace: loans-underwriting
spec:
  selector:
    app: loans-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer